# Release notes

One file per tag, named `vX.Y.Z.md`. The release workflow refuses to publish a
tag without a matching file, so the notes are written before the tag exists
rather than reconstructed from the commit log afterwards.

| Version | Summary |
|---|---|
| [v0.1.0](v0.1.0.md) | First release: exec, env, ls, doctor, refresh, login, rescope, adopt, export/import, schedule, self-update |
